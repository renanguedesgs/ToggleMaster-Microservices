apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: targeting-application
  namespace: argocd
  labels:
    app: targeting-service
spec:
  project: default

  source:
    repoURL: https://github.com/renanguedesgs/ToggleMaster-Microservices
    targetRevision: main
    path: k8s/apps/targeting-service

  destination:
    server: https://kubernetes.default.svc
    namespace: targeting-ns

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
