apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: evaluation-application
  namespace: argocd
  labels:
    app: evaluation-service
spec:
  project: default

  source:
    repoURL: https://github.com/renanguedesgs/ToggleMaster-Microservices
    targetRevision: main
    path: k8s/apps/evaluation-service

  destination:
    server: https://kubernetes.default.svc
    namespace: evaluation-ns

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
